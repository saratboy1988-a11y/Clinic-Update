Place the Git for Windows installer in this folder before building ClinicManager.iss.

Required filename:
GitInstaller.exe

Example:
deps\GitInstaller.exe

The installer will skip Git automatically if Git is already installed.
